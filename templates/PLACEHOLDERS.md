# Placeholder ทั้งหมดในระบบ ConsDocx

อ้างอิงสำหรับตอนทำเอกสารเพิ่ม (เอกสาร 8, 9, ...) — เอา placeholder ที่มีอยู่แล้วไปใช้ซ้ำได้เลย
ไม่ต้องคิดชื่อใหม่ ถ้าความหมายตรงกับที่มีอยู่แล้ว

**ที่อยู่ไฟล์เทมเพลต:** `templates/ขออนุมัติโครงการ/` (ชุดขออนุมัติโครงการ เอกสาร 1–7)
โฟลเดอร์ `templates/ราคากลาง/` เป็นชุดเอกสารราคากลาง ยังไม่ได้ต่อเข้าโปรแกรม

รูปแบบ placeholder ในไฟล์ `.docx`: พิมพ์ข้อความ `[ชื่อ_placeholder]` ธรรมดาลงในเอกสาร (มีวงเล็บเหลี่ยมครอบ)
โค้ดฝั่ง JS จะค้นแทนที่ข้อความนี้ด้วยค่าจริงตอนสร้างไฟล์

**ตัวเลขในเอกสาร: ใช้เลขอารบิก (0-9) เท่านั้น** — เทมเพลตทุกไฟล์ถูกแปลงเลขไทย ๐-๙ เป็นอารบิกแล้ว
(2026-08-13, 2,922 ตัว ใน 35 ไฟล์) พร้อมเปลี่ยนเลขลำดับอัตโนมัติใน `numbering.xml` จาก `thaiNumbers`
เป็น `decimal` 12 จุด — ถ้าทำเทมเพลตใหม่อย่าพิมพ์เลขไทย และอย่าตั้งรูปแบบเลขข้อเป็นเลขไทยใน Word
หมายเหตุ: ชื่อฟอนต์ `TH SarabunIT๙` มีเลขไทยอยู่ในชื่อ ห้ามแปลง (สคริปต์แปลงเฉพาะข้อความใน `<w:t>`)
ส่วนช่องกรอกในหน้าเว็บยังพิมพ์เลขไทยได้ — `ConsDocxMonthYear` แปลงเป็นอารบิกให้เองก่อนใส่ลงเอกสาร

**ที่มาของค่าตอนสร้างไฟล์:** ทุกช่องอยู่ที่ `forms/main.html` หน้าเดียว (หน้าฟอร์มแยกรายเอกสารถูกยุบ
มารวมหมดแล้ว) ค่าทั้งหมดอ่านจากช่องในหน้านี้ผ่าน `val("g_...")` — ชื่อ id ที่เขียนในตารางข้างล่าง
คือ id ของช่องในหน้านี้

## Placeholder ที่ใช้ร่วมกันหลายเอกสาร

| Placeholder | ความหมาย | ใช้ในเอกสาร | ค่ามาจาก |
|---|---|---|---|
| `[ชื่อโครงการ]` | ชื่อโครงการก่อสร้าง | 1, 2, 3, 4, 5, 6, 7 | ช่อง "ชื่อโครงการ" หน้า main (`projectName`) |
| `[รายละเอียดโครงการ]` | รายละเอียดโครงการ (ขนาด/ปริมาณงาน/สถานที่ ฯลฯ) | 3, 6 | ช่อง "รายละเอียดโครงการ" หน้า main (`g_projectDetail`) |
| `[เดือน]` | เดือนที่ออกเอกสาร | 2, 3, 7 | ช่อง "เดือน/ปี" หน้า main (`g_month`) |
| `[ปี]` | ปี พ.ศ. ที่ออกเอกสาร | 2, 3, 7 | ช่อง "เดือน/ปี" หน้า main (`g_year`) |
| `[จำนวนเงินตัวเลข]` | งบประมาณ ตัวเลขพร้อม comma | 3, 4, 5 | ช่อง "งบประมาณ" หน้า main (`g_moneyNum`) ผ่าน `formatComma()` |
| `[จำนวนเงินตัวอักษร]` | งบประมาณ แปลงเป็นตัวอักษรไทย | 3, 4, 5 | ช่อง "งบประมาณ" หน้า main (`g_moneyNum`) ผ่าน `bahtText()` |

บรรทัด "สั่ง ณ วันที่ ___ เดือน [เดือน] พ.ศ.[ปี]" ของเอกสาร 2 และ 7 ใช้ 2 ตัวนี้ (วันที่เว้นว่างให้เขียนมือ)

หมายเหตุ `[เดือน]`/`[ปี]`: เอกสาร 1, 4, 5, 6 ไม่มี placeholder นี้ในเทมเพลตแล้ว (ใช้ `[วันที่หนังสือ...]` ซึ่งมี
เดือน/ปีอยู่ในตัวแทน) แต่โค้ดยังส่งค่าให้ทุกเอกสารอยู่ ไม่ต้องแก้อะไรถ้าจะเอา `[เดือน]`/`[ปี]` กลับมาใช้ในเทมเพลต

หมายเหตุ `[รายละเอียดโครงการ]`: ตอนนี้มีแค่เทมเพลต 3 ที่พิมพ์ไว้ แต่โค้ดส่งค่าให้ทุกเอกสารผ่าน
`sharedProjectFields()` — พิมพ์ลงเทมเพลตอื่นได้เลยโดยไม่ต้องแก้โค้ด

## เลขที่/วันที่หนังสืออ้างอิง (4 คู่) — ใช้ร่วมกันหลายเอกสาร

กรอกที่การ์ด "เลขที่/วันที่หนังสือที่เกี่ยวข้อง" (`g_orderNo`, `g_orderDate`, `g_letter1No/Date`,
`g_letter2No/Date`, `g_letter3No/Date`) — ชุดเดียวใช้ร่วมกันทุกเอกสาร

| Placeholder | ความหมาย | ใช้ในเอกสาร | ฟิลด์ |
|---|---|---|---|
| `[เลขที่คำสั่งคณะกรรมการจัดทำแบบรูปรายการงานก่อสร้าง]` | เลขที่คำสั่งแต่งตั้งคณะกรรมการ (เอกสาร 2) | 2, 3, 5 | `orderNo` |
| `[วันที่คำสั่งคณะกรรมการจัดทำแบบรูปรายการงานก่อสร้าง]` | วันที่ของคำสั่งนั้น | 3, 5 | `orderDate` |
| `[เลขที่หนังสือขออนุมัติโครงการ]` | เลขที่หนังสือของเอกสาร 1 | 1, 5 | `letter1No` |
| `[วันที่หนังสือขออนุมัติโครงการ]` | วันที่ออกหนังสือของเอกสาร 1 | 1, 5 | `letter1Date` |
| `[เลขที่หนังสือรายงานการจัดทำแบบรูปรายการ]` | เลขที่หนังสือของเอกสาร 4 | 4 | `letter2No` |
| `[วันที่หนังสือรายงานการจัดทำแบบรูปรายการ]` | วันที่ออกหนังสือของเอกสาร 4 | 4, 5 | `letter2Date` |
| `[เลขที่หนังสือขอนำส่งเอกสารการจัดทำโครงการ]` | เลขที่หนังสือของเอกสาร 5 | 5, 6 | `letter3No` |
| `[วันที่หนังสือขอนำส่งเอกสารการจัดทำโครงการ]` | วันที่ออกหนังสือของเอกสาร 5 | 5, 6 | `letter3Date` |
| `[เลขที่คำสั่งคณะกรรมการราคากลาง]` | เลขที่คำสั่งแต่งตั้งคณะกรรมการกำหนดราคากลาง (เอกสาร 7) | 7 | `g_rcOrderNo` |

**กลไกเว้นว่าง** (ฟังก์ชัน `docRefNoWithYear()` / `docRefNoOnly()` / `docRefDate()` + ค่าคงที่
`DOC_REF_BLANK` = ช่องว่าง 8 ตัว) — ทุกช่องเว้นว่างได้ ถ้าไม่กรอกจะได้ช่องว่างให้เขียนมือทีหลังแทน:

- เลขที่คำสั่ง (`orderNo`, `g_rcOrderNo`) → `"␣␣␣␣␣␣␣␣/{ปี}"` — เติมปี พ.ศ. ต่อท้ายให้อัตโนมัติ
  (ถ้ากรอกเอง ใช้ข้อความที่พิมพ์ตรงๆ ไม่เติมปีให้)
- เลขที่หนังสืออื่น (`letter1No`, `letter2No`, `letter3No`) → `"␣␣␣␣␣␣␣␣"` เปล่าๆ (เลขที่หนังสือมี
  ส่วนหน้าเป็นชื่อส่วนราชการพิมพ์ไว้ในเทมเพลตอยู่แล้ว จึงไม่เติมปี)
- วันที่ทุกช่อง → `"␣␣␣␣␣␣␣␣{เดือน} {ปี}"` — เติมเดือน/ปีจากช่อง "เดือน/ปี" ให้อัตโนมัติ

**ช่องวันที่พิมพ์ย่อได้** — `ConsDocxMonthYear.formatThaiDate(raw, ปีสำรอง)` ใน `shared/monthyear.js`
แปลงให้เป็นวันที่เต็มภาษาไทยเสมอก่อนใส่ลงเอกสาร รับได้ทั้ง `1/2/2569`, `1-2-69`, `1.2.2569`,
`2569-02-01`, `1 ก.พ. 69`, `1 กพ 2569`, `1 กุมภา 2569`, เลขไทย `๑/๒/๒๕๖๙` → ทั้งหมดได้
`1 กุมภาพันธ์ 2569` — ปี 2 หลักเติม 2500 ให้, ปี ค.ศ. (1900–2200) บวก 543 ให้, ไม่พิมพ์ปีก็ใช้ปีจากช่อง
"เดือน/ปี" ถ้าแกะรูปแบบไม่ออกจะคืนข้อความเดิมที่พิมพ์มา (ไม่แก้ให้เพี้ยน) หน้า main ยังแปลงให้เห็นในช่อง
ทันทีตอนคลิกออกจากช่อง (`input.date-field` + event `blur`)

## เรื่องเดิม / ข้อเท็จจริง (เอกสาร 1 และ 5 เท่านั้น)

| Placeholder | ความหมาย | ค่ามาจาก |
|---|---|---|
| `[PM1]` `[PM2]` `[PM3]` `[PM4]` | ย่อหน้า "เรื่องเดิม" — แต่ละอันอยู่คนละย่อหน้าในเอกสาร | ช่อง "เรื่องเดิม" หน้า main (`g_priorMatter`) |
| `[ข้อเท็จจริง]` | ข้อความ "ข้อเท็จจริง" | ช่อง "ข้อเท็จจริง" หน้า main (`g_facts`) |

**กลไกพิเศษของ PM1-4:** ถ้าผู้ใช้พิมพ์ข้อความเองในช่อง "เรื่องเดิม" ข้อความนั้นจะถูกใส่ใน `[PM1]`
อย่างเดียว ส่วนย่อหน้า `[PM2]`-`[PM4]` (ที่ปกติมีข้อความตัวอย่าง/default อยู่) จะถูก**ลบทั้งย่อหน้าทิ้ง**
ไปเลย ไม่ใช่แค่เว้นว่าง — ถ้าผู้ใช้ไม่พิมพ์อะไรเลย จะใช้ข้อความ default 4 ท่อน (คงที่ในโค้ด `DEFAULT_PM_1`
ถึง `DEFAULT_PM_4`) ใส่ใน 4 ย่อหน้าตามเดิม

เหตุที่ต้องแยกเป็น 4 placeholder แทนที่จะเป็นก้อนเดียว: เทมเพลตต้นฉบับเขียนย่อหน้า "เรื่องเดิม" เป็น
4 บรรทัดแยกกัน (แต่ละบรรทัดอ้างอิงกฎหมาย/มติ/งบประมาณคนละเรื่อง) เวลาผู้ใช้กรอกเอง มักมีแค่เนื้อหาเดียว
เลยยุบเหลือย่อหน้าเดียว (PM1) แล้วลบ 3 ย่อหน้าที่เหลือทิ้ง

ถ้าจะทำ placeholder แบบ "ลบทั้งย่อหน้าถ้าไม่มีค่า" ในเอกสารใหม่ ให้ส่งค่า `null` (ไม่ใช่ `""`) เข้า
`fieldsMap` ตอนเรียก `buildOneDocx()` — โค้ดจะลบ `<w:p>...</w:p>` ทั้งก้อนที่มี placeholder นั้นทิ้งให้

## เอกสาร 2, 3, 4 — คณะกรรมการจัดทำแบบรูปรายการงานก่อสร้าง

| Placeholder | ความหมาย | ค่ามาจาก |
|---|---|---|
| `[ชื่อกรรมการ1]` `[ชื่อกรรมการ2]` `[ชื่อกรรมการ3]` | ชื่อ-สกุลกรรมการ 3 คน | หน้า main (`g_name1/2/3`) |
| `[ตำแหน่งกรรมการ1]` `[ตำแหน่งกรรมการ2]` `[ตำแหน่งกรรมการ3]` | ตำแหน่งกรรมการ 3 คน | หน้า main (`g_pos1/2/3`) |

หมายเหตุ: เอกสาร 4 ตัดยศ/คำนำหน้าแบบ "รักษาการ" ออกจากชื่อ (เช่น "ว่าที่ ร.ต.") ด้วยฟังก์ชัน
`stripActingRank()` ก่อนแทนที่ `[ชื่อกรรมการN]` — เอกสาร 2, 3 ไม่ตัด

## เอกสาร 2 และ 3 — อ้างอิงงบประมาณ

| Placeholder | ความหมาย | ค่ามาจาก |
|---|---|---|
| `[อ้างอิงงบประมาณ]` | อ้างอิงงบประมาณในย่อหน้า "ด้วยกองช่าง..." (เอกสาร 2 ใช้ซ้ำ 3 จุด เพราะมี 3 แผ่นเหมือนกัน) | ช่อง "ประเภทงบประมาณ"/"ปีงบประมาณ" (`g_budgetType`/`g_budgetYear`) — ประกอบเป็น "{ประเภท} ประจำปีงบประมาณ พ.ศ. {ปี}" โดย `budgetType`: ordinance = "ตามข้อบัญญัติงบประมาณ" / saving = "ตามโครงการใช้จ่ายเงินสะสม" |

ฟังก์ชันที่ประกอบข้อความนี้: `budgetRefText2()` (ใช้กับทั้งเอกสาร 2 และ 3) ถ้าไม่ระบุปี จะ default
เป็นปี พ.ศ. ปัจจุบัน (`ConsDocxMonthYear.currentYearThai()`)

## เอกสาร 3 เท่านั้น

| Placeholder | ความหมาย | ค่ามาจาก |
|---|---|---|
| `[ชื่อผู้จดรายงานการประชุม]` `[ตำแหน่งผู้จดรายงานการประชุม]` | ผู้จดรายงานการประชุม | `g_recName`/`g_recPos` |
| `[ชื่อผู้ตรวจรายงานการประชุม]` `[ตำแหน่งผู้ตรวจรายงานการประชุม]` | ผู้ตรวจรายงานการประชุม | = ประธานกรรมการชุดเดียวกับ `[ชื่อกรรมการ1]`/`[ตำแหน่งกรรมการ1]` (ตัดยศด้วย `stripActingRank()` เฉพาะชื่อ เพราะเอกสารมีคำว่า "ว่าที่ ร.ต." พิมพ์ไว้คงที่ตรงบรรทัดลงชื่ออยู่แล้ว) |
| `[เวลาเริ่มประชุม]` | เวลาเริ่มประชุม (ใช้ซ้ำ 2 จุด — หัวเอกสารและบรรทัด "เริ่มประชุมเวลา") | `g_meetStart` — เดิม hardcode "13.30" |
| `[เวลาเลิกประชุม]` | เวลาเลิกประชุม (บรรทัด "เลิกประชุมเวลา") | `g_meetEnd` — เดิม hardcode "14.00" |

## เอกสาร 5 เท่านั้น

| Placeholder | ความหมาย | ค่ามาจาก |
|---|---|---|
| `[จำนวนวันแล้วเสร็จ]` | จำนวนวันที่กำหนดให้งานแล้วเสร็จ | `g_workDays` |
| `[ประเภทงานก่อสร้าง]` | ประเภทงานก่อสร้าง (ตามหลักเกณฑ์ราคากลาง) | `g_constrType` |
| `[ประเภทงานค่าK]` | ประเภทงานสำหรับคำนวณค่า K | `g_kWorkType` |
| `[ลักษณะงานที่ใช้สูตรค่าK]` | ลักษณะงานย่อยที่ใช้สูตรค่า K | `g_kSubWork` |
| `[สูตรค่าK]` | สูตรคำนวณค่า K | `g_kFormula` |
| `[ชื่อกรรมการราคากลาง1/2/3]` `[ตำแหน่งกรรมการราคากลาง1/2/3]` | คณะกรรมการกำหนดราคากลางงานก่อสร้าง (ข้อ 8.1) — เอกสาร 7 ใช้ชุดเดียวกันนี้ | หน้า main (`g_rc1/2/3name`, `g_rc1/2/3pos`) |
| `[ชื่อกรรมการตรวจรับ1/2/3]` `[ตำแหน่งกรรมการตรวจรับ1/2/3]` | คณะกรรมการตรวจรับพัสดุในงานจ้างก่อสร้าง (ข้อ 8.2) | หน้า main (`g_ic1/2/3name`, `g_ic1/2/3pos`) |
| `[ชื่อผู้ควบคุมงาน1/2]` `[ตำแหน่งผู้ควบคุมงาน1/2]` | ผู้ควบคุมงานก่อสร้าง (ข้อ 8.3) | หน้า main (`g_sv1/2name`, `g_sv1/2pos`) |

## เอกสาร 6 — บันทึกขออนุมัติแต่งตั้งคณะกรรมการกำหนดราคากลาง

ไม่มี placeholder เฉพาะของตัวเอง ใช้ชุดร่วมล้วนๆ — `[ชื่อโครงการ]`, `[รายละเอียดโครงการ]` และ
`[เลขที่หนังสือขอนำส่งเอกสารการจัดทำโครงการ]` / `[วันที่หนังสือขอนำส่งเอกสารการจัดทำโครงการ]`
(สร้างด้วย `buildDoc6()` ที่ spread `sharedProjectFields()` แล้วส่ง `[เดือน]`/`[ปี]` เพิ่มไว้เผื่อใช้)

## เอกสาร 7 — คำสั่งแต่งตั้งคณะกรรมการกำหนดราคากลาง

| Placeholder | ความหมาย | ค่ามาจาก |
|---|---|---|
| `[เลขที่คำสั่งคณะกรรมการราคากลาง]` | เลขที่คำสั่งฉบับนี้ (พิมพ์ซ้ำ 3 จุด เพราะเอกสารมี 3 แผ่นเหมือนกัน) | ช่อง "เลขที่คำสั่งคณะกรรมการกำหนดราคากลาง" การ์ด "เลขที่/วันที่หนังสือที่เกี่ยวข้อง" (`g_rcOrderNo`) |
| `[ชื่อกรรมการราคากลาง1/2/3]` `[ตำแหน่งกรรมการราคากลาง1/2/3]` | คณะกรรมการกำหนดราคากลาง — ชุดเดียวกับเอกสาร 5 ข้อ 8.1 | หน้า main (`g_rc1/2/3name`, `g_rc1/2/3pos`) |
| `[เดือน]` `[ปี]` | บรรทัด "สั่ง ณ วันที่ ___ เดือน [เดือน] พ.ศ.[ปี]" (3 จุด — เอกสารมี 3 แผ่นเหมือนกัน) | ช่อง "เดือน/ปี" หน้า main |
| `[วิธีจัดซื้อจัดจ้าง]` | ย่อหน้า "...โดย[วิธีจัดซื้อจัดจ้าง] นั้น" (3 จุด) — เดิมพิมพ์ "โดยวิธีเฉพาะเจาะจง" ตายตัวไว้ | คิดจากงบประมาณ (`g_moneyNum`) ให้อัตโนมัติ + เลือกทับได้ที่ `g_procMethod` — ดู `procMethodCode()` / `PROC_METHOD_TEXT` |

## ข้อความ "ค่าตั้งต้น" ที่โปรแกรมใส่ให้เอง — ออกมาเป็นตัวอักษรสีแดง

ช่องไหนที่ผู้ใช้เว้นว่างไว้แล้วโปรแกรมเติมข้อความให้แทน จะถูกแทนลงเอกสารด้วย **ตัวอักษรสีแดง**
(`<w:color w:val="FF0000"/>`) เพื่อให้เห็นชัดว่าต้องกลับมาตรวจ/แก้ก่อนใช้จริง ถ้าผู้ใช้กรอกเองจะเป็นสีปกติ

ตอนนี้ที่เป็นสีแดงได้: `[PM1]`-`[PM4]` (ข้อความ "เรื่องเดิม" ตัวอย่าง), `[ข้อเท็จจริง]` (ข้อความตัวอย่าง),
และเลขที่/วันที่หนังสือทุกช่องที่เว้นว่างไว้ (ช่องว่าง 8 ตัว + ปี/เดือน-ปีที่เติมให้)

**วิธีทำให้ค่าใหม่เป็นสีแดง:** ห่อค่าด้วย `asDefault(...)` ตอนใส่ลง `fieldsMap` เช่น
`"[ข้อเท็จจริง]": val("g_facts") || asDefault(DEFAULT_FACTS)` — ค่าปกติ (string) ยังใช้ได้เหมือนเดิม
ไม่ต้องแก้อะไร กลไกข้างในคือ `asDefault()` คืน `{ text, isDefault: true }` แล้ว `buildOneDocx()` แกะออก
ส่งต่อให้ `replacePlaceholder(xml, ph, value, isDefault)` ซึ่งจะเรียก `rPrAsDefault(rPr)` ใส่ `<w:color>`
สีแดงเข้าไปใน `<w:rPr>` ของ **เฉพาะรันที่แทนค่าลงไป** เท่านั้น (ข้อความเดิมรอบๆ ในรันเดียวกันไม่เปลี่ยนสี)
และคงรูปแบบเดิมของรัน (ฟอนต์/ขนาด/ตัวหนา) ไว้ครบ

หมายเหตุตำแหน่ง `<w:color>`: ลำดับ element ใน `<w:rPr>` มีผลกับ Word — `rPrAsDefault()` จึงแทรก
`<w:color>` ไว้ก่อน `w:spacing`/`w:sz`/`w:u`/`w:lang` ฯลฯ ให้เอง และลบ `<w:color>` เดิม (เช่น `w:val="auto"`)
ออกก่อนเสมอ

## ฟังก์ชันช่วยแปลงค่า (ใช้ซ้ำได้ทุกเอกสารใหม่)

ทั้งหมดอยู่ใน `forms/main.html` (ไฟล์เดียว):

- `sharedProjectFields(data, projectName, monthG, yearG)` — คืน placeholder ชุด
  "ข้อมูลโครงการ" ทั้งหมด (`[ชื่อโครงการ]`, `[รายละเอียดโครงการ]` + เลขที่/วันที่หนังสือ 4 คู่) spread ลงใน
  `fieldsMap` ของทุกเอกสาร — เอกสารใหม่ให้ spread ตัวนี้เข้าไปด้วยเสมอ
- `formatComma(numStr)` — ใส่ comma คั่นหลักพัน เช่น `2256000` → `2,256,000`
- `bahtText(numStr)` — แปลงตัวเลขเป็นข้อความจำนวนเงินภาษาไทย เช่น `2256000` → `สองล้านสองแสนห้าหมื่นหกพันบาทถ้วน`
- `stripActingRank(name)` — ตัดคำนำหน้า/ยศรักษาการ (เช่น "ว่าที่ ร.ต.") ออกจากชื่อ
- `xmlEscape(s)` — escape อักขระพิเศษ (`&`, `<`, `>`) ก่อนแทรกลง XML ของ docx
- `normalizePlaceholderRuns(xml)` — เรียกก่อนแทนที่ placeholder ทุกครั้งเสมอ (สำคัญ ดูหัวข้อถัดไป)

## ข้อควรระวัง: Word อาจตัด placeholder ขาดเป็นหลายส่วนในไฟล์ .docx

เวลาพิมพ์ `[ชื่อ_placeholder]` ลงใน Word แล้ว Word บันทึกไฟล์ บางครั้ง Word จะเก็บข้อความนี้แยกเป็นหลาย
"run" (`<w:r>`) ในไฟล์ XML ข้างใน — มักเกิดตรงรอยต่อภาษาไทย/อังกฤษปนกัน (เช่น `[สูตรค่าK]` ที่มีทั้งไทยและ
อังกฤษ) หรือบางทีก็เกิดกับข้อความไทยล้วนๆ อย่าง `[PM1]` ด้วยเหมือนกัน ถ้าไม่จัดการเรื่องนี้ โค้ดแทนที่
placeholder (`replacePlaceholder()`) จะหา `[ชื่อ_placeholder]` ไม่เจอ (เพราะข้อความไม่ได้อยู่ใน `<w:t>`
เดียวกัน) แล้ว placeholder นั้นจะโผล่ค้างเป็นข้อความ `[...]` เดิมๆ ในไฟล์ที่สร้างออกมา ไม่ถูกแทนที่เลย
(เจอบั๊กนี้จริงกับ `[สูตรค่าK]` ในเอกสาร 5 มาแล้ว)

วิธีแก้: เรียก `normalizePlaceholderRuns(xml)` ก่อนเสมอ (ก่อน loop แทนที่ placeholder ทุกตัว) — ฟังก์ชันนี้
จะไล่สแกน run ทีละตัวแล้วรวม run ที่มี "[" ค้างอยู่ (ยังไม่เจอ "]" ปิด) เข้ากับ run ถัดๆ ไปจนกว่าจะเจอ "]"
ให้กลายเป็น run เดียว ใช้ได้กับ placeholder ทุกชื่อโดยไม่ต้อง hardcode รายชื่อ placeholder ไว้ล่วงหน้า —
ถ้าทำเอกสารใหม่แล้วพบว่า placeholder ตัวไหนไม่ถูกแทนที่ (ยังเห็น `[...]` ค้างในไฟล์ที่สร้างออกมา) ให้สงสัย
เรื่องนี้ก่อนเป็นอันดับแรก แล้วเช็คว่าเรียก `normalizePlaceholderRuns()` ก่อน loop แทนที่หรือยัง

## เอกสารราคากลาง 8–11 (ต่อเข้าโปรแกรมแล้ว 2026-08-13 — เทมเพลต 9-11 ยังไม่มี placeholder)

ชุดเอกสารราคากลางอยู่ที่ `templates/ราคากลาง/<ประเภทงาน>/<วิธี>/` ยังไม่ได้ต่อเข้าโปรแกรม
มี 3 มิติที่ทำให้เนื้อหาต่างกัน แต่**แยกไฟล์เทมเพลตแค่ 2 มิติแรก = 9 ชุด** มิติที่ 3 จัดการด้วย placeholder

### โครงสร้างโฟลเดอร์ (จัดไว้แล้ว 2026-08-12 — 33 ไฟล์ ครบ 9 ชุด)

```
templates/ราคากลาง/
├── อาคาร/  ├── ทาง/  └── ชลประทาน/          ← ประเภทงาน (ไม่มีคำว่า "งาน" นำหน้า)
    ├── เฉพาะเจาะจง/   8, 9, 10, 11
    ├── ebidding/      8, 9, 10, 11
    └── egp/           8, 9, 10  (ไม่มี 11 แบบ บก.)
```

กติกา:

1. **เลขนำหน้าไฟล์เป็น 8, 9, 10, 11 เหมือนกันทุกชุด** (ไม่ใช้ 8.2/9.2/10.2 แล้ว) เลขเดียวกัน = บทบาทเดียวกัน
   — 8 รายงานการประชุม, 9 รายงานราคากลาง, 10 นำส่ง, 11 แบบ บก.
2. ข้อความหลังเลขตั้งตามชื่อจริงของชุดนั้นได้ ไม่ต้องเหมือนกันข้ามชุด — **ชื่อไฟล์ที่ผู้ใช้ดาวน์โหลดจะใช้ชื่อ
   ไฟล์เทมเพลตนั้นเอง** (ตัด `_template` ออก) จึงไม่ต้องมี filename ในโค้ด
3. ทุกไฟล์ต้องเป็น `.docx` จริง (ไฟล์ `.doc` เดิมถูกแปลงแล้ว) และลงท้าย `_template.docx`
4. ชุด `egp` ไม่มีเอกสาร 11 — โปรแกรมต้องแสดงรายการเท่าที่ชุดนั้นมี ไม่ error
5. slug ที่ใช้ตั้งชื่อไฟล์ JS: `อาคาร→building`, `ทาง→road`, `ชลประทาน→irrigation`,
   `เฉพาะเจาะจง→specific` (ส่วน `ebidding`/`egp` ใช้ชื่อเดิม) — ตรงกับรหัสที่ `procMethodCode()` คืนค่า

### การแยกไฟล์ JS (โหลดเฉพาะชุดที่ใช้)

เอกสาร 1–7 อยู่ใน `forms/main-template-data.js` เหมือนเดิม (โหลดตอนเปิดหน้า เพราะใช้เกือบทุกครั้ง)
ส่วนราคากลางแยกเป็น 9 ไฟล์ที่ `forms/template-data/rc-<work>-<method>.js` — โครงการหนึ่งใช้แค่ชุดเดียว
ถ้ารวมเป็นไฟล์เดียวจะโตหลาย MB ทุกครั้งที่เปิดหน้า

```js
window.RC_TEMPLATES = window.RC_TEMPLATES || {};
window.RC_TEMPLATES["building/specific"] = {
  8: { name: "8. รายงานการประชุมคณะกรรมการกำหนดราคากลาง", b64: "UEsDBBQ..." },
  ...
};
```

โหลดด้วยการแทรก `<script src>` ตอนกดสร้างครั้งแรกของชุดนั้น (ไม่ใช้ `fetch` เพื่อให้เปิดจาก `file://` ได้)
แล้วจำไว้ไม่โหลดซ้ำ

| มิติ | ค่าที่เป็นไปได้ | จัดการโดย |
|---|---|---|
| ประเภทงาน | งานอาคาร / งานทาง / งานชลประทาน | **แยกโฟลเดอร์เทมเพลต** — ผู้ใช้เลือกเองจาก dropdown (ไม่ผูกกับหมวดค่า K) |
| วิธีจัดซื้อจัดจ้าง | เฉพาะเจาะจง / e-bidding / e-GP | **แยกโฟลเดอร์เทมเพลต** — โปรแกรมคิดจากงบประมาณให้ แต่เลือกทับเองได้ |
| ราคากลางเทียบงบประมาณ | ไม่สูงกว่า / สูงกว่า | **placeholder ในเทมเพลตเดียว** (ดูข้างล่าง) |

**กฎเลือกวิธีจัดซื้อจัดจ้างจากงบประมาณ** (`g_moneyNum` — งบประมาณเบื้องต้น ไม่ใช่ราคากลาง):

| งบประมาณ | วิธี |
|---|---|
| < 500,000 | เฉพาะเจาะจง |
| 500,000 – < 1,000,000 | e-bidding (500,000 พอดี = e-bidding) |
| ≥ 1,000,000 | e-GP (1,000,000 พอดี = e-GP) |

ราคากลางที่คำนวณได้ **ไม่มีผลกับการเลือกวิธี** แม้จะข้ามเส้นแบ่งก็ยังใช้วิธีที่คิดจากงบประมาณ

### ช่องกรอกในหน้า main (ทำครบแล้ว)

หน้า main แบ่งเป็น **2 แท็บ**: "ขออนุมัติโครงการ (เอกสาร 1–7)" กับ "ราคากลาง (เอกสาร 8–11)" — การ์ดแต่ละใบ
ติด `data-tab="approve" | "rc" | "both"` ไว้ ตัวที่เป็น `both` (รายชื่อที่ใช้บ่อย, ข้อมูลโครงการ,
ราคาค่าก่อสร้างเบื้องต้น, คณะกรรมการกำหนดราคากลาง) เห็นทั้ง 2 แท็บ จำแท็บล่าสุดไว้ใน localStorage
`consdocx_tab` เพราะงานราคากลางทำคนละช่วงเวลากับตอนขออนุมัติ

| id | ชนิด | ความหมาย |
|---|---|---|
| `g_rcWorkCat` | select | ประเภทงาน: อาคาร/ทาง/ชลประทาน — เลือกเอง (ไม่ผูกกับหมวดค่า K) |
| `g_procMethod` | select | วิธีจัดซื้อจัดจ้าง (อยู่การ์ด "ราคาค่าก่อสร้างเบื้องต้น") — คิดจากงบให้ + เลือกทับได้ |
| `g_rcPrice` | text (ตัวเลข) | ราคากลางที่คณะกรรมการคำนวณได้ (คนละตัวกับ `g_moneyNum`) — มีช่องตัวอักษรแปลงให้ + บอกผลต่างจากงบให้เห็นทันที |
| `g_rcRecName` / `g_rcRecPos` | text | ผู้จดรายงานการประชุมราคากลาง |
| `g_rcMeetStart` / `g_rcMeetEnd` | text | เวลาเริ่ม/เลิกประชุมราคากลาง |

`g_rcWorkCat` + วิธีที่ได้ = ชุดเทมเพลตที่ใช้ (เช่น `building/specific`) แสดงให้เห็นในช่อง "ชุดเอกสารที่จะใช้"
และหัวการ์ด "รายการเอกสารราคากลาง" ทางขวา

### placeholder ที่โปรแกรมส่งให้เอกสาร 8–11 ทุกฉบับแล้ว (ฟังก์ชัน `rcFields()`)

พิมพ์ชื่อพวกนี้ลงเทมเพลต 9, 10, 11 ได้เลย ไม่ต้องแก้โค้ดเพิ่ม:

| Placeholder | ค่าที่ได้ |
|---|---|
| ชุดร่วมทั้งหมดจาก `sharedProjectFields()` | `[ชื่อโครงการ]` `[รายละเอียดโครงการ]` เลขที่/วันที่หนังสือ 4 คู่ `[เลขที่คำสั่งคณะกรรมการราคากลาง]` `[วิธีจัดซื้อจัดจ้าง]` |
| `[เดือน]` `[ปี]` | เดือน/ปีที่เลือกหน้า main |
| `[จำนวนเงินตัวเลข]` `[จำนวนเงินตัวอักษร]` | **งบประมาณเบื้องต้น** (`g_moneyNum`) |
| `[ราคากลางตัวเลข]` `[ราคากลางตัวอักษร]` | **ราคากลางที่คำนวณได้** (`g_rcPrice`) |
| `[ผลต่างราคากลางตัวเลข]` `[ผลต่างราคากลางตัวอักษร]` | ค่าสัมบูรณ์ของ (ราคากลาง − งบประมาณ) |
| `[ชื่อกรรมการราคากลาง1-3]` `[ตำแหน่งกรรมการราคากลาง1-3]` | คณะกรรมการกำหนดราคากลาง (ชุดเดียวกับเอกสาร 5 และ 7) |
| `[ชื่อผู้จดรายงานการประชุมราคากลาง]` `[ตำแหน่งผู้จดรายงานการประชุมราคากลาง]` | `g_rcRecName` / `g_rcRecPos` |
| `[ชื่อผู้ตรวจรายงานการประชุมราคากลาง]` `[ตำแหน่งผู้ตรวจรายงานการประชุมราคากลาง]` | ประธานคณะกรรมการราคากลาง (`g_rc1name` ตัดยศด้วย `stripActingRank()` / `g_rc1pos`) |
| `[เวลาเริ่มประชุมราคากลาง]` `[เวลาเลิกประชุมราคากลาง]` | `g_rcMeetStart` / `g_rcMeetEnd` |
| `[เดือนราคาวัสดุ]` `[ปีราคาวัสดุ]` | เดือน/ปีที่ใช้ราคาวัสดุก่อสร้าง (เอกสาร 8 ข้อ 6.1) — `g_rcMatMonth` / `g_rcMatYear` คนละตัวกับเดือน/ปีที่ออกเอกสาร |
| `[กรณีราคากลางไม่สูงกว่างบประมาณ]` `[กรณีราคากลางสูงกว่างบประมาณ]` | พิมพ์ข้อความไว้ทั้ง 2 ย่อหน้าในเทมเพลต โปรแกรมส่ง `null` ให้อันที่ไม่ตรงเงื่อนไข → **ย่อหน้านั้นถูกลบทั้งย่อหน้า** (เท่ากันพอดี = ไม่สูงกว่า) |

**ราคากลางเท่ากับงบประมาณพอดี** → นับรวมกับกรณี "ไม่สูงกว่างบประมาณ"

ข้อความที่ต่างกันตามประเภทงาน (เช่น ย่อหน้าในรายงานการประชุม) ใช้วิธีเดียวกับ `[อ้างอิงงบประมาณ]` คือให้
โค้ดประกอบข้อความตามประเภทงานที่เลือก — **ยังไม่ได้กำหนดเงื่อนไข รอสรุปภายหลัง**

### placeholder ที่พิมพ์ไว้แล้วในเอกสาร 8 (ชุดเฉพาะเจาะจง ทั้ง 3 ประเภทงาน)

ใช้ของเดิมได้เลยไม่ต้องแก้โค้ด: `[ชื่อโครงการ]` `[รายละเอียดโครงการ]` `[เดือน]` `[ปี]`
`[จำนวนเงินตัวเลข]` `[จำนวนเงินตัวอักษร]` `[ชื่อกรรมการราคากลาง1-3]` `[ตำแหน่งกรรมการราคากลาง1-3]`

ตัวใหม่ที่ต้องเพิ่มช่องกรอก/โค้ด:

| Placeholder | ค่าที่ควรได้ |
|---|---|
| `[ชื่อผู้จดรายงานการประชุมราคากลาง]` `[ตำแหน่งผู้จดรายงานการประชุมราคากลาง]` | ช่องใหม่ (คู่ขนานกับ `g_recName`/`g_recPos` ของเอกสาร 3) |
| `[ชื่อผู้ตรวจรายงานการประชุมราคากลาง]` `[ตำแหน่งผู้ตรวจรายงานการประชุมราคากลาง]` | เสนอให้ใช้ประธานคณะกรรมการราคากลาง (`g_rc1name`/`g_rc1pos`) อัตโนมัติ แบบเดียวกับเอกสาร 3 |
| `[เวลาเริ่มประชุมราคากลาง]` `[เวลาเลิกประชุมราคากลาง]` | ช่องใหม่ (คู่ขนานกับ `g_meetStart`/`g_meetEnd`) |

~~เทมเพลตมี `[เวลาเริ่มประชุม]` ปนอยู่ 1 จุด (บรรทัดหัวเอกสาร)~~ **แก้แล้ว 2026-08-12** เป็น
`[เวลาเริ่มประชุมราคากลาง]` ทั้ง 3 ไฟล์ — ตอนนี้เอกสาร 8 ใช้ `[เวลาเริ่มประชุมราคากลาง]` 2 จุด
(หัวเอกสาร + บรรทัด "เริ่มประชุมเวลา") และ `[เวลาเลิกประชุมราคากลาง]` 1 จุด

เอกสาร 9, 10, 11 และชุด ebidding/egp ยังไม่มี placeholder เลย (ยังเป็นเอกสารต้นฉบับ)

### เรื่องที่ต้องเคลียร์ก่อนลงมือเขียนโค้ด
- ~~เอกสาร 7 พิมพ์ "โดยวิธีเฉพาะเจาะจง" ตายตัวไว้ในเทมเพลต~~ **แก้แล้ว 2026-08-12** เป็น
  `โดย[วิธีจัดซื้อจัดจ้าง]` ทั้ง 3 แผ่น พร้อมช่อง `g_procMethod` และกฎคิดจากงบประมาณในโค้ด
- ถ้อยคำ e-GP: ตอนนี้ `PROC_METHOD_TEXT.egp` ใช้ข้อความเดียวกับ e-bidding — **รอยืนยันถ้อยคำที่ถูกต้อง**
- ขนาดไฟล์: base64 ของ 9 ชุด × 3–4 ฉบับ (~36 ไฟล์) จะทำให้ `main-template-data.js` โตจาก ~350KB
  เป็นระดับหลาย MB โหลดหน้าเว็บช้า — ต้องเลือกวิธีก่อนเขียน เช่น แยกไฟล์ JS ต่อชุดแล้วโหลดเฉพาะชุดที่ใช้
  หรือ `fetch()` ไฟล์ .docx ตอน runtime (ใช้ได้เพราะ deploy บน GitHub Pages แต่เปิดจาก `file://` จะพัง)

## ขั้นตอนเพิ่มเอกสารใหม่ (เช่น เอกสาร 8)

1. สร้างไฟล์ `.docx` ต้นแบบใน `templates/ขออนุมัติโครงการ/` **ตั้งชื่อขึ้นต้นด้วยเลขเอกสาร** (`8. ...docx`)
   แล้วพิมพ์ placeholder ที่ต้องการ — **ใช้ชื่อ placeholder ที่มีอยู่แล้วในตารางข้างต้นถ้าความหมายตรงกัน**
   (เช่น `[ชื่อโครงการ]`, `[เดือน]`, `[ปี]`) เพื่อให้ปุ่ม "สร้างไฟล์ทั้งหมด" ที่หน้า main กรอกให้อัตโนมัติได้เลย
   โดยไม่ต้องแก้โค้ดเพิ่ม
2. regenerate base64: `python tools/gen-template-data.py` — สคริปต์ไล่อ่านไฟล์ `.docx` ทุกไฟล์ในโฟลเดอร์นั้น
   (เรียงตามเลขนำหน้าชื่อไฟล์) แล้วเขียน `TEMPLATE_N_BASE64` ลง `forms/main-template-data.js` ให้ครบทุกฉบับ —
   **ต้องรันทุกครั้งที่แก้เทมเพลต** ไม่งั้นแอปจะยังใช้เนื้อหาเก่าอยู่ (แอปไม่เคยอ่านไฟล์ `.docx` ตอน runtime เลย
   อ่านจาก base64 ในไฟล์นี้อย่างเดียว)
3. เพิ่มรายการ + ปุ่ม "สร้างไฟล์" ใน `<ul class="doclist">` ที่ `forms/main.html` (ใส่ `data-doc="N"`)
   แล้วเพิ่มฟังก์ชัน `buildDocN()` (ดูรูปแบบจาก `buildDoc1`-`buildDoc7`) — อย่าลืม spread
   `sharedProjectFields(...)` ไว้บนสุดของ `fieldsMap`, ลงทะเบียนใน `DOC_INFO` และเพิ่มใน
   ปุ่ม "สร้างไฟล์ทั้งหมด" (`genAll`) ด้วย
4. ถ้ามีช่องกรอกใหม่ ให้เพิ่ม `<input id="g_...">` ในการ์ดที่เกี่ยวข้อง แล้วใส่ id ลงใน `AUTOSAVE_FIELDS`
   ด้วย ไม่งั้นค่าที่กรอกจะไม่ถูกจำไว้ตอนปิดหน้า
5. ถ้าค่าไหนเป็น "ค่าตั้งต้นที่โปรแกรมใส่ให้เอง" ให้ห่อด้วย `asDefault(...)` จะได้ออกมาเป็นตัวอักษรสีแดง
